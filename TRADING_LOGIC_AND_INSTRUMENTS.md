# 通道均值回歸 DCA 網格 — 交易邏輯與商品規格

本文件只記錄「目前實際運作的版本」：精確的進出場邏輯、資金與風控參數、以及目前交易的
12 檔商品規格。驗證過程、抓到的 bug、版本演變history 記錄在
[strategy_logic_and_backtest_truth.md](strategy_logic_and_backtest_truth.md)，本文件不重複。

- 對應程式：`fx_engine_v2.py`（回測）／`ChannelGridDCA_EA.mq5`（實盤 EA，v3.5；回測引擎 v3.6）
- 兩者用同一套邏輯、同一個 broker 時區基準（MT5 broker 伺服器時間，實測約 UTC+3）

---

## 1. 進出場邏輯（狀態機）

週期：**4H**，指標：**EMA50**（中軌）、**ATR20**（波動度，TR 的簡單移動平均，非 Wilder 平滑）。

指標一律取**前一根已收盤**的 4H K棒（EA 端即 `CopyBuffer(handle, 0, 1, 1, buf)` 的 `shift=1`），
第 i 根的訊號只用得到第 i−1 根收盤時就已知的資訊，不使用尚未走完的當前 K 棒（不重繪、無未來函數）。
回測引擎自 v3.2 起與 EA 一致（舊版誤用當根指標比對當根 high/low，已修正）。

**4H K棒的組成排除 broker 00:00 那一根 1H。** 那是換日結算、全日流動性最低的時段，
點差擴大到平常的十幾倍，且資料源在該根有系統性假跳空（跳空中位數是其他時段的 50～80 倍）。
因此 00:00–04:00 這根 4H 由 01/02/03 三根 1H 組成，避免用不可成交的尖刺價觸發進出場。

```
狀態：空手
  若 Low  <= EMA50 − 2.0×ATR   → 做多，建立第 1 層
  若 High >= EMA50 + 2.0×ATR   → 做空，建立第 1 層

狀態：持有多單（第 1～4 層）
  出場目標 = min( EMA50, 均價 + 1.0×ATR )      ← 兩個目標哪個先到就出，不必等回到中軌
  硬停損   = 均價 − 4.0×ATR

  若 High >= 出場目標           → 全部平倉（止盈）
  否則若 Low <= 硬停損           → 全部平倉（停損）
  否則若尚未達 4 層              → 在「均價 − 層數×1.5×ATR」加碼

狀態：持有空單 → 方向對稱（出場目標 = max(EMA50, 均價−1.0×ATR)，停損 = 均價+4.0×ATR）
```

**硬停損是唯一的風控機制，任何情況下都不可拿掉**——這是本策略驗證過程中犯過的錯誤
（詳見 `strategy_logic_and_backtest_truth.md` 第 2 節），EA 啟動時會主動檢查
`InpStopAtr<=0` 並拒絕啟動。

### 加碼金額規則

首單 $1,500（或帳戶淨值 6%），**每一層加碼都固定是首單的 1.2 倍（$1,800）**，不是逐層複利
放大。4 層滿倉總名目 = $1,500 + $1,800×3 = $6,900（本金的 27.6%）。

---

## 2. 部位大小與手數換算

不是固定手數，是依「目標美元名目」動態換算，正確處理跨幣別（交叉盤）：

```
單位數 = 目標美元名目 ÷ 基礎貨幣的美元匯率
手數   = 單位數 ÷ 100,000
損益   = 單位數 × 價格變動 × 計價貨幣的美元匯率
```

EA 端則用 MT5 原生提供的 `1手名目 = (tick_value ÷ tick_size) × 現價` 換算，兩者結果一致，
`tick_value` 已內建跨幣別轉換，因此 AUDCAD、GBPCAD 這類交叉盤也能算出正確手數
（早期版本用「美元金額÷報價」的簡化公式，對非 USD 計價的交叉盤會算錯，已修正）。

手數向下取整到經紀商的 lot step，避免超額下單；低於最小手數時會提高到最小值並記錄警告。

---

## 3. 資金與風控參數

| 參數 | 數值 | 說明 |
|---|---|---|
| 本金 | $25,000 | 回測基準；可等比放大 |
| 首單名目 | 淨值的 6%（≈$1,500） | `InpBaseOrderPct` |
| 加碼倍數 | 1.2× 首單（固定，非複利） | `InpSizeMultiplier` |
| 最大層數 | 4 層 | `InpMaxLayers` |
| 進場通道 | EMA50 ± 2.0×ATR | `InpChannelK` |
| 加碼間距 | 每層 1.5×ATR | `InpDcaStepAtr` |
| 止盈 | min(EMA50, 均價±1.0×ATR) | `InpTpAtr` |
| 硬停損 | 均價 ∓ 4.0×ATR | `InpStopAtr`，不可為 0 |
| 點差過濾 | 即時點差 > 該對中位數×3 禁止新開倉 | `InpMaxSpreadMult` |
| 總曝險上限 | 所有部位名目合計 ≤ 淨值 60%（可設 0 =不設限） | `InpMaxTotalRiskPct` |
| 佣金 | $3/手/邊（來回 $6） | `COMMISSION_PER_LOT_SIDE` |
| 隔夜利息 | 依各對真實 Swap，週三三倍 | 見第 4 節 |
| 方向 | 雙向（做多＋做空） | `InpAllowLong` / `InpAllowShort` |

---

## 4. 交易商品（12 檔，依 Sharpe 排序）

來源：G8 貨幣（USD/EUR/GBP/JPY/CHF/CAD/AUD/NZD）兩兩組合的**全部 28 檔**（major+minor+cross）
逐一回測後取 Sharpe 最高的一批，再經人工判斷微調（見下方 v3.4 說明）。資料為 TradingView 的
Pepperstone 真實報價（`PEPPERSTONE:xxx`），1.67 年樣本，成本含真實點差＋佣金＋隔夜利息，
28 檔全數為實測值。

| 貨幣對 | Sharpe | Calmar | 年化 | MDD(盤中) | MDD(收盤) | 點差(pips) |
|---|---|---|---|---|---|---|
| AUDCAD | 2.12 | 1.82 | 1.07% | 0.59% | 0.56% | 1.10 |
| AUDCHF | 1.99 | 2.27 | 1.34% | 0.59% | 0.53% | 0.80 |
| NZDCHF | 1.88 | 1.85 | 1.25% | 0.68% | 0.56% | 1.10 |
| AUDUSD | 1.74 | 1.83 | 1.28% | 0.70% | 0.69% | 0.30 |
| EURAUD | 1.56 | 1.55 | 0.96% | 0.62% | 0.58% | 1.00 |
| GBPNZD | 1.53 | 1.44 | 0.77% | 0.53% | 0.51% | 2.20 |
| CADCHF | 1.40 | 2.19 | 0.85% | 0.39% | 0.36% | 0.90 |
| GBPCHF | 1.24 | 1.56 | 0.69% | 0.45% | 0.42% | 1.30 |
| NZDUSD | 1.23 | 1.48 | 1.05% | 0.71% | 0.71% | 0.50 |
| NZDCAD | 1.11 | 1.97 | 0.83% | 0.42% | 0.41% | 1.20 |
| GBPCAD | 1.10 | 0.96 | 0.56% | 0.58% | 0.54% | 1.50 |
| EURCHF | 1.09 | 1.18 | 0.48% | 0.41% | 0.35% | 0.80 |

28 檔全數為實測點差，無估計值。MDD 自 v3.6 起以**盤中最不利價**計價（見下方 v3.6 說明），
收盤計價數字併列供對照。

### v3.6：回撤改用盤中計價、並揭露第 4 層加不到

**① MDD 由收盤計價改為盤中計價（2.06% → 2.87%）。**
舊版用每根 4H K棒的收盤價計算未平倉部位浮動損益，但 K 棒收盤前的浮虧往往深得多，
而那才是盯盤時真正看到的數字。現在改用盤中最不利價（多單取 low、空單取 high）。
策略本身沒有任何改變，變的只是「用什麼價格承認浮虧」。Calmar 也因此由 5.25 降到 3.75。

需要註明：盤中計價是**保守上界**——它假設所有部位在同一瞬間都摸到各自最差點，
實務上不太可能同時發生。真實體感落在 2.06%~2.87% 之間，但寧可用難看的那個數字規劃資金。

**② `InpMaxLayers=4` 實際上等於 3。**
第 4 層的觸發距離是 `3 × 1.5 = 4.5×ATR`，硬停損在 `4.0×ATR`——停損永遠先到，
第 4 層結構上加不到。全期 2,118 筆交易中**一筆 4 層的都沒有**，證實了這點。

實測對照過三種設定，**維持現狀最好**：

| 設定 | 年化 | MDD(收盤) | Sharpe | Calmar |
|---|---|---|---|---|
| 只做 1 層 | 5.74% | 1.46% | 2.70 | 3.92 |
| 最多 2 層 | 9.39% | 2.21% | 2.66 | 4.26 |
| **最多 3 層（現狀）** | **10.79%** | **2.06%** | **2.81** | **5.25** |
| step 1.2 讓第4層可達 | 11.31% | 2.28% | 2.63 | 4.97 |
| 停損放寬到 5.0 | 10.54% | 3.11% | 2.33 | 3.39 |

值得注意的是：單看交易紀錄，3 層交易淨虧 $2,788（勝率僅 45%），很容易誤判成
「第 3 層在賭博」。但那是**選擇偏誤**——需要加到第 3 層的本來就是走深的單子，
不加碼它們不會消失，而是變成均價更差的 2 層單、更容易撞停損。對照組證實了這點：
砍成 2 層後 MDD 反而升到 2.21%。**加第 3 層是在救那些單，不是在放大賭注。**

### v3.5：再加入 NZDCAD### v3.5：再加入 NZDCAD（11 檔 → 12 檔）

NZDCAD 點差 1.2 pips 為實測值，Sharpe 1.20、Calmar 2.12，屬中段偏上的標的。加入後組合年化
10.13% → 10.95%（+0.82pp），Calmar 5.43 → 5.33、MDD 1.87% → 2.06%——跟 v3.4 同一種取捨：
多一檔分散部位換更高絕對報酬，風險調整後績效小幅下降。這次沒有額外的人工判斷成分，純粹是
「多加一個實測成本已知、統計特徵中上」的標的做分散。

### v3.4：拿掉 CADJPY，加入 CADCHF、GBPCAD（人工判斷，非排名結果）

前幾版標的都是純用歷史 Sharpe 排名決定，這次不同——**CADJPY 被主動拿掉**，理由是近期
JPY 走勢趨勢性明顯變強，不利於均值回歸邏輯。它的權益曲線也看得到佐證：2026年8月有一筆
單次虧損 $103 的停損（加碼到 3 層後跟不上趨勢），MDD 0.83% 是拿掉前 10 檔中最差的。
換上的 CADCHF（Sharpe 1.39、Calmar 2.36，全場數一數二平滑）與 GBPCAD（Sharpe 1.06，
2026-09 才補實測點差、原本因估計值過高被排除在候選外）。

**這不是純改善的取捨**：組合年化從 9.68% 升到 10.13%，但 Calmar 從 5.77 降到 5.43
（MDD 1.68% → 1.87%）——用一點風險調整後績效換更高的絕對報酬。回測資料能驗證「歷史上
CADJPY 表現如何」，但無法驗證「JPY 未來是否持續趨勢」，這部分是使用者對近期市場狀態的
主觀判斷，不是資料驅動的結論。

**28 檔全部使用實測點差，已無任何估計值**（v3.3，2026-09-09 重跑
`ExportForexRealCosts.mq5`，清單由 21 檔擴到 28 檔）。原本用 2.5 pips 估計值代入的三檔
（GBPNZD、GBPCHF、NZDCHF），真實值全部更窄——也就是先前是低估而非高估績效；同時發現
CADCHF 的估計值曾高估近三倍（估 2.5 pips、實測僅 0.90），一度因此被錯誤排除在候選外。

調查方法的穩定性也一併驗證：21 檔前後兩次（不同週）調查的點差差異全部在 0.3 pips 內。

### 淘汰／未採用的商品

| 貨幣對 | Sharpe | 平滑度 | 狀態 |
|---|---|---|---|
| CADJPY | 1.23 | 0.85 | v3.4 主動移除（JPY 近期趨勢性強，不利均值回歸；見上） |
| AUDNZD | −0.32 | −0.64 | 負報酬，權益曲線與時間相關係數為負，純雜訊 |
| GBPAUD | −0.04 | 0.35 | 報酬幾乎為零 |
| USDCHF | 0.25 | 0.20 | 平滑度全場最低，走勢崎嶇 |

「平滑度」= 權益曲線數值與時間索引的 Pearson 相關係數，越接近 1 代表越像單調斜線上升
（如 AUDCAD、AUDCHF），越接近 0 或負值代表純雜訊或反覆震盪。完整 28 檔掃描結果見
`fx_28pairs_scan_results.csv` 與 `fx_28pairs_scan_grid.png`。

---

## 5. 組合層級績效（12 檔共用資金池，$25,000 本金，1.67 年）

| 指標 | 數值 |
|---|---|
| 年化報酬 | 10.76% |
| 最大回撤（盤中計價） | 2.87% |
| 最大回撤（收盤計價，對照） | 2.06% |
| Sharpe | 2.80 |
| Calmar | 3.75 |
| 勝率 | 93.5% |
| 交易次數 | 2,118 筆 |
| 平均／峰值動用資金 | 61.8% ／ 162.3% |

峰值動用資金超過 100%，代表這是**保證金槓桿**而非現金交易。

### 「目前回撤」與「目前浮動損益」的差別

網頁上這兩格是不同的東西，這個策略特別容易搞混：

| 指標 | 量的是什麼 |
|---|---|
| 目前回撤 | 權益比**歷史最高點**低多少 |
| 目前浮動損益 | **未平倉部位**現在帳面浮虧多少 |

因為本策略 89% 的時間都掛著浮虧在跑（平均約本金 1.4%，見
`strategy_logic_and_backtest_truth.md` 第 2.8 節），**權益高點那一刻本身就含著浮虧**，
所以回撤永遠小於浮虧，差額就是高點當時的浮虧深度。實例：

```
權益最高點(2026-09-11 12:00)  權益 29,797   浮虧 −$11.60
現在                          權益 29,696   浮虧 −$115.84
                              ─────────────────────────
                              回撤 −$101    浮虧多了 −$104
```

**拿回撤去估風險會低估。** FTMO 這類資金計畫看的是含浮虧的**權益**，
該盯的是浮動損益那一格，不是回撤。

這是**回測結果**，反映的是策略邏輯在真實歷史報價下的統計特徵，不是對未來的保證。
樣本僅 1.67 年、選股本身是全樣本事後排名（v3.4 再疊加一層人工判斷）——完整的方法論限制與
誠實揭露見 `strategy_logic_and_backtest_truth.md` 第 4、5 節。數字會隨 GitHub Actions
每小時自動重跑而變動，本文件只記錄寫成當下的快照，即時數字以網頁為準。

---

## 6. 資料與部署

| 項目 | 內容 |
|---|---|
| 價格資料 | TradingView Pepperstone 真實報價，1H 抓取後重採樣為 4H |
| 時區基準 | MT5 broker 伺服器時間（動態量測換算，見 `fx_data_pepperstone.py`） |
| 回測引擎 | `fx_engine_v2.py`（單檔）／`fx_portfolio_v2.py`（12 檔共用資金池組合） |
| 實盤 EA | `ChannelGridDCA_EA.mq5`，已部署至 MT5 `MQL5/Experts/` |
| 自動更新 | GitHub Actions 每小時重抓報價、重跑回測、重新產生網頁（`.github/workflows/hourly_update.yml`） |
| 即時網頁 | `docs/index.html`（GitHub Pages）／Claude Artifact，含即時持倉、目前回撤、逐筆交易明細 |

### ⚠️ v3.4 上線前要做的事（既有 CADJPY 部位）

移除 CADJPY 後，EA 主迴圈只跑 `InpSymbols` 清單裡的商品，**不會再管理任何已從清單移除的
持倉**——不更新停損、不檢查止盈、不加碼。若你目前有實盤 CADJPY 部位，換上新版 EA 之前
請先手動決定：直接手動平倉，或是保留但自行盯著（EA 不會再幫你顧）。
