# 通道均值回歸網格策略：完整邏輯與真實成本驗證紀錄

本文取代舊版《網格與通道 DCA 策略邏輯拆解與回測真實度深度分析》。舊版描述的策略邏輯本身沒有錯，
但其宣稱的績效（勝率 85~92%、年化 40~80%、MDD 1~2%、夏普 3.5~4.7）**無法在真實資料與嚴格邏輯下重現**。
本文記錄：策略的精確定義、驗證過程中抓到的具體錯誤、真實資料下的實際表現，以及最終建議配置。

---

## 1. 策略核心邏輯（狀態機，唯一標準定義）

以下是本文件承認的唯一策略邏輯，之後所有績效數字都必須對應這個定義，任何偏離都要明講差異。

兩個貫穿全篇的前提（v3.2 起回測與實盤 EA 一致，見第 2.4 節）：
**EMA/ATR 一律取前一根「已收盤」4H K 棒**（不用當根，無未來函數）；
**4H K 棒不含 broker 00:00 那根 1H**（換日結算時段，點差十幾倍且資料有假跳空）。

```
狀態：空手
  若 Low <= EMA50 - 2.0×ATR                          → 建立第 1 層多單（做多方向）
  若 High >= EMA50 + 2.0×ATR   （僅雙向版本才有此規則）→ 建立第 1 層空單

狀態：持有多單（1~4 層）
  若 High >= 出場目標                                 → 全部平倉（止盈）
      出場目標：
        - md 原版：EMA50（等到完全回到中軌）
        - 較近止盈版：min(EMA50, 均價 + 1.0×ATR)（哪個先到就出，其餘邏輯不變）
  否則若 Low <= 均價 - 4.0×ATR                        → 全部平倉（硬停損，任何版本都不可省略）
  否則若尚未達 4 層                                    → 在「均價 - 層數×1.5×ATR」加碼，
                                                        每層固定加碼 首單×1.2 = $1,800（不是複利成長）

狀態：持有空單 → 與多單方向對稱
```

固定參數：本金 $25,000、首單名目 $1,500、每層加碼 $1,800、最大 4 層（滿倉名目 $6,900 = 本金 27.6%）、
通道 2.0 ATR、加倉間距 1.5 ATR、止盈/停損用 20 期 ATR 與 50 EMA。**硬停損是這個策略唯一的風控機制，
拿掉它等於拿掉整個策略的下檔保護。**

### 部位大小怎麼算（v2 修正）

不是固定手數，是依「目標美元名目」換算：

```
單位數 = 目標美元名目 ÷ 該基礎貨幣的美元匯率     ← v2 修正處
手數   = 單位數 ÷ 100,000
損益   = 單位數 × 價格變動 × 該計價貨幣的美元匯率  ← v2 修正處
```

**v1 的錯誤**：舊引擎用 `美元金額 ÷ 報價` 算單位數，這只有在計價貨幣是 USD 時才成立。
CADJPY 這類日圓計價交叉盤算出來的單位數小了約 150 倍，無法直接換算成 MT5 手數。
百分比報酬不受影響（損益÷投入 = 價格變動百分比，與幣別無關），但手數與隔夜利息/佣金的絕對金額會失真。

MT5 EA 用 `1手名目 = (tick_value ÷ tick_size) × 現價` 換算，`tick_value` 由平台提供且已含跨幣別轉換，
所以交叉盤也會得到正確手數。實際手數（$25,000 帳戶）落在 **0.012 ~ 0.032 手**之間。

---

## 2. 為什麼舊版數字不可信：驗證過程中抓到的具體錯誤

這不是空泛的懷疑，是逐一比對程式碼、用同一份資料做 A/B 測試後的結論。

### 2.1 `backtest_mt5_real_all_costs.py`：偷偷放寬止盈條件

程式碼把止盈條件從「碰到 EMA50」改成「碰到 均價+1×ATR 或 EMA50，哪個先到」，但**停損邏輯完全沒變**。
用同一份 2015-2026 年 CFD 真實逐根點差資料、同 13 檔標的做 A/B 對照：

| 標的 | 該script宣稱年化 | 拿掉bug後真實年化 | 該script MDD | 真實MDD |
|---|---|---|---|---|
| AMD | 115.31% | **2.27%** | 1.47% | **11.55%** |
| PLTR | 112.57% | **-13.13%** | 1.99% | **17.27%** |
| TSLA | 155.96% | **-10.21%** | 5.68% | **20.18%** |
| AAPL | 131.73% | **8.83%** | 1.06% | **4.39%** |

13 檔裡 8 檔從「大賺」翻成「淨虧損」。**提早止盈本身不是問題**（見第 4 節，外匯上這其實是個有效優化），
問題是它會系統性減少「撐到真正停損」的機會，讓策略看起來勝率更高、卻同時悄悄放大了真實回撤。

### 2.2 `backtest_fx_4h_8h_all_pairs.py`：整個停損分支被刪掉

多單狀態機只剩「止盈」與「加倉」兩個分支，**沒有第三個處理停損的 `elif`**。加滿 4 層後若繼續跌，
部位會無限期掛著等反彈，不會被強制平倉，也不會被算進「已平倉交易」統計。實測 7 檔貨幣對、
合計 1715 筆交易：

```
EURUSD/USDJPY/EURGBP/EURJPY/GBPJPY/CADJPY/CHFJPY
→ 平倉勝率 100.0%，虧損筆數 = 0
```

這在統計上不可能是真實停損機制下的結果——唯一解釋是虧損部位從未被平倉，只用未實現虧損拖累權益曲線
（回撤數字還在，只是不會出現在「勝率」與「已平倉交易」統計裡）。此外空單開倉完全沒有扣現金
（多單有正確扣款，空單沒有），等於空單那一半的策略沒付摩擦成本；且 18 檔清單因批次下載限流靜默失敗
11 檔，只剩最主流的 7 檔存活，但標題仍宣稱「18 檔全市場」。

### 2.3 兩者共通問題

- 用 CFD 合成資料或只挑長期強勢股測試，迴避了 MSTR、GME、T、NKE、AIRF 這類常敗軍。
- 各標的各自獨立 $25,000 本金，若把多檔淨利潤直接加總會等於偷換成數倍本金的結果。

### 2.4 自己的引擎也抓到兩個（v3.2 修正）

前面兩節是別人寫的腳本，這節是**我們自己這套引擎**被實盤對帳抓出來的問題。
起因是拿 MT5 的真實持倉去比對網頁：回測顯示 AUDCHF、GBPNZD 已經止盈平倉，
實盤兩檔卻都還開著。查下去是兩件獨立的事。

**① 用換日假跳空觸發止盈（虛增淨利 6.7%）**

broker 00:00（＝21:00 UTC）是換日結算、全日流動性最低的一小時，點差擴到平常的十幾倍。
實測資料源在這一根有系統性假跳空——「開盤對前根收盤」的跳空中位數是其他時段的 50～80 倍：

```
各 4H 時段開盤跳空中位數（pips）
AUDCHF   00h=8.20   04h=0.10  08h=0.10  12h=0.10  16h=0.10  20h=0.10
GBPNZD   00h=10.00  04h=0.30  08h=0.20  12h=0.20  16h=0.20  20h=0.20
CADJPY   00h=5.30   04h=0.10  08h=0.10  12h=0.10  16h=0.10  20h=0.10
```

最大跳空到 254 pips（GBPNZD）。舊版拿這根尖刺的 high/low 觸發進出場，會生出實盤拿不到的
止盈：全期 1,710 筆裡有 **80 筆止盈只有這根尖刺碰得到**（之後三小時再也沒回到那個價位），
金額佔全期淨利 **6.7%**，而且**全是止盈、零停損**——停損距離 4×ATR 太遠，尖刺只夠灌水、
不夠傷人，是單向偏誤。修法：重採樣成 4H 前先丟掉這根 1H，00:00–04:00 改由 01/02/03 組成
（`fx_engine_v2.EXCLUDE_ROLLOVER_HOUR`）。

**② 指標用當根、卻拿去比對當根的 high/low（輕微未來函數）**

舊版 `add_indicators()` 沒有 shift，第 i 根的 EMA/ATR 含第 i 根的收盤價，卻拿去跟第 i 根的
high/low 比對進出場。實盤 EA 呼叫 `CopyBuffer(handle, 0, 1, 1, buf)`，`shift=1` 指前一根
**已收盤**的 K 棒，不可能用到還在跑的當根。修法：EMA/ATR 一律 `.shift(1)`，跟 EA 對齊。

**結果**：年化 11.18% → 9.47%，但 MDD 2.38% → 1.69%、Calmar 4.69 → 5.62。
修正後回測的未平倉部位與實盤帳戶**七檔方向全對、五檔連 DCA 層數都一致**（修正前只有兩檔）。
用修正後的引擎重掃 28 檔，Top-10 有 9 檔不變，選股沒有被這兩個 bug 帶歪。

**教訓**：這兩個 bug 都不是靠讀程式碼發現的，是靠**拿實盤持倉逐筆對帳**逼出來的。
回測看起來越漂亮，越該去對帳。

### 2.5 估計成本也是一種未驗證假設（v3.3）

v3.1 換成 Top-10 時，GBPNZD、GBPCHF、NZDCHF 三檔沒有實測點差，用 2.5 pips 的「保守估計」
代入。事後把 `ExportForexRealCosts.mq5` 的清單從 21 檔擴到 28 檔重跑，真實值是
**GBPNZD 2.20、GBPCHF 1.30、NZDCHF 1.10 pips——全部比估計更窄**，也就是先前是
*低估*績效，不是高估。NZDCHF 因此從第 6 名升到第 3 名（Sharpe 1.48 → 1.87）。

比排名變動更重要的是 **CADCHF**：它的估計值 2.5 pips 相對真實值 0.90 高估了近三倍，
在全成本重掃後直接從榜外升到第 7 名。**一個錯誤的成本假設可以讓一檔完全可用的商品
在篩選階段就被淘汰掉**——而且因為它從沒進過名單，這種錯誤不會被實盤對帳發現。

同時也驗證了調查方法本身：21 檔在不同週做的兩次調查，點差差異全部落在 0.3 pips 以內
（多數為 −0.1），代表這個「7 天 × 每天 48 時段取樣中位數」的統計是穩定可複製的。

### 2.6 v3.4：第一次不是資料驅動的標的變動

前面所有標的變動（v3.1 選 Top-10、v3.3 補實測成本）都是同一套規則：跑回測、排 Sharpe、
取前幾名。v3.4 不一樣——是使用者主動判斷「CADJPY 近期 JPY 走勢趨勢性變強，不適合均值
回歸策略」，要求拿掉它，換上 CADCHF、GBPCAD。

這個判斷**沒辦法用歷史回測驗證**，因為它是對「未來」市場狀態的判斷，不是對「過去」的
統計檢驗。誠實的作法是承認這一點，而不是硬凹一個回測數字去支持它。能做的只有：

1. **看歷史資料裡有沒有佐證**：CADJPY 的權益曲線在 2026 年 8 月確實有一次很清楚的斷崖式
   下跌——加碼到 3 層後遇到單邊趨勢、最終停損 $103，MDD 0.83% 是拿掉前 10 檔中最差的。
   這條曲線的形狀（前段陡峭上升、尾端被巴一巴掌）跟「趨勢突然變強、均值回歸失效」的
   假說是吻合的，但一次事件不足以證明「JPY 未來會持續這樣」，只能說「不衝突」。
2. **算清楚換掉的代價**：組合年化 9.68%→10.13%（+0.45pp），但 Calmar 5.77→5.43
   （−0.34），MDD 1.68%→1.87%。單獨拿掉 CADJPY 不補人是最差的（Calmar 掉到 4.97），
   是加回 CADCHF+GBPCAD 才拉回來——換句話說，這次操作的正報酬主要來自「多兩個相對更穩定
   的商品分散風險」，跟「CADJPY 本身變差」是兩件獨立的事，不該混為一談。

**操作風險提醒**：CADJPY 從 `InpSymbols` 移除後，EA 主迴圈不會再管理任何既有的 CADJPY
持倉（不更新停損、不檢查止盈、不加碼）——如果換版前帳上還有 CADJPY 部位，必須先手動處理，
否則它會變成一個沒人看管的孤兒倉位。

### 2.7 v3.5：加入 NZDCAD，回到純資料驅動

跟 v3.4 不同，這次沒有主觀判斷成分——NZDCAD 點差 1.2 pips 是實測值，Sharpe 1.20、
Calmar 2.12，統計特徵在 28 檔裡屬中段偏上，加進來單純是多一個獨立訊號來源做分散。

效果跟 v3.4 是同一個模式：組合年化 10.13%→10.95%（+0.82pp），Calmar 5.43→5.33、
MDD 1.87%→2.06%。**這已經是連續第二次「加標的換報酬、犧牲一點風險調整後績效」**，
值得注意的是峰值動用資金也跟著持續墊高：v3.3 的 10 檔是 140.5%，v3.4 的 11 檔是
149.1%，v3.5 的 12 檔來到 162.3%。標的數量增加本身會機械性地推高同時在場部位的槓桿
曝險，這不是策略變差，但實盤時要確認保證金水位跟得上，尤其在 InpMaxTotalRiskPct 已經
設為不設限（0）的情況下。

---

## 3. 真實資料下的實際表現

### 3.1 美股（15 分鐘 / 4H / 日線）

用 IBKR 真實股票 1 年期資料（15m）與 MT5 真實逐根點差 CFD 資料（2022 起，4H／日線），
完全照第 1 節的 md 原版邏輯（無提早止盈、保留停損）：

| 資料/週期 | 平均年化報酬 | 平均勝率 | 平均夏普 | 平均MDD |
|---|---|---|---|---|
| IBKR 真實股票，15m（1年，45檔） | -0.39% | 77.7% | -0.25 | 2.98% |
| IBKR 真實股票，4H（1年） | 0.93% | 86.6% | 0.82 | 2.17% |
| CFD 真實點差，4H（4.7年） | 0.54% | 82.6% | 0.32 | 4.98% |
| CFD 真實點差，日線（4.7年） | 1.03% | 86.9% | 0.43 | 4.06% |

**勝率數字大致可信（82~92%，接近舊版宣稱），但年化報酬只有 -0.4%~1.0%，離「40~80%」差了近百倍。**
負偏態（贏多次小錢、偶爾賠一次大錢）加上被壓縮的交易頻率，是報酬遠低於宣稱值的核心原因。
高波動/非典型上升趨勢標的（MSTR、GME、AIRF、T、NKE、RACE、BABA）在所有週期都是常敗軍。

### 3.2 外匯（4H，這是最終落地的資產類別）

外匯沒有股票的長期向上趨勢紅利，理論上更適合純粹的均值回歸邏輯，實測也確實如此：

**18 檔全測（md 原版邏輯，只做多，估計點差）**：平均年化 0.76%、平均勝率 86.6%、17/18 檔獲利、
平均 MDD 0.91%（唯一虧損：EURCHF，長期受 SNB 干預）。

---

## 4. 優化方向：哪些有效、哪些是雜訊

用前 60% 資料訓練、後 40% 資料驗證的方式逐一檢查：

| 方向 | 結果 | 判定 |
|---|---|---|
| 一個帳戶同時跑多檔（共用資金池） | 年化 0.28%→6.00%，夏普 0.55→1.36 | ✅ 有效，效果最大 |
| 較近止盈 `min(EMA50, 均價+1ATR)`，**保留停損** | 同樣年化下 MDD 降 24%、資金週轉快一倍 | ✅ 有效 |
| 雙向交易（外匯適用） | 需搭配較近止盈才有效，單獨加做空無幫助 | ✅ 條件性有效 |
| 用歷史 Sharpe/Calmar 排名挑標的 | 樣本內外相關性 **-0.03~-0.12**，等於雜訊 | ❌ 無效，但仍採用（見第5節說明） |
| 參數調校（通道倍數/停損/加倉間距） | 48 組參數樣本內外相關性 -0.118，全是過擬合 | ❌ 無效 |
| EMA200 趨勢過濾 | 年化不升反降，資金在場率暴跌到 5.8% | ❌ 無效 |
| 時間停損 | 40~90 根從不觸發（平均持倉僅 8 根），無意義 | ❌ 無效 |
| 悲觀盤中撮合假設（同根K棒優先觸發停損） | 結果與樂觀假設完全相同 | ✅ 穩健性通過 |
| 放大部位（首單 $1,500→$4,000） | 年化等比放大，夏普不變，但槓桿到本金 394% | ⚠️ 是槓桿不是 alpha |

**標的篩選的誠實結論**：15 個月的樣本內表現最好的 6 檔，在樣本外表現反而不如當初最差的 6 檔
（相關性 -0.025，等於零）。第 5 節的標的名單是「用全樣本排出來的最佳結果＋人工判斷」，本質仍是事後篩選，
不是被驗證過的可延續選股法則——只是結構性排除規則（ATR/點差比、政策干預標的）站得住腳。

---

## 5. 最終建議配置與驗證後績效

### 配置

```
標的：12 檔（G8 全 28 檔 major/minor/cross 掃描後依 Sharpe 排名，v3.4 再經人工判斷微調）
      AUDCAD · AUDCHF · GBPNZD · GBPCHF · EURCHF · NZDUSD · EURAUD · AUDUSD · NZDCHF · CADCHF · GBPCAD · NZDCAD
      (v3.3 起 28 檔全部使用 ExportForexRealCosts.mq5 實測點差，無估計值；
       v3.4 拿掉 CADJPY、加入 CADCHF/GBPCAD，見第 2.6 節；v3.5 再加入 NZDCAD，見第 2.7 節)
週期：4H
邏輯：第 1 節狀態機 + 雙向交易 + 較近止盈 min(EMA50, 均價±1ATR) + 保留 4ATR 硬停損
帳戶：單一 $25,000（或等比放大）同時交易全部 12 檔，共用資金池
成本：MT5 broker 真實歷史點差（過去7天、每天48時段取樣中位數，MQL5腳本
      ExportForexRealCosts.mq5 實測）+ ECN 佣金 + 真實隔夜利息（週三三倍）
```

### 驗證後績效（1.67 年真實 Pepperstone 歷史資料，$25,000 本金，v3.5 · 12 檔）

| 指標 | 數值 | v3.4（11檔） | v3.3（10檔） | v3.1（修正前） |
|---|---|---|---|---|
| 年化報酬 | **10.95%** | 10.13% | 9.68% | 11.18% |
| 最大回撤 (MDD) | **2.06%** | 1.87% | 1.68% | 2.38% |
| 目前回撤（截至最新更新） | 0.09% | 0.10% | 0.71% | 0.39% |
| Sharpe | **2.85** | 2.90 | 2.90 | 3.18 |
| Calmar（年化報酬÷MDD） | **5.33** | 5.43 | 5.77 | 4.69 |
| 勝率 | 93.4% | 93.3% | 93.5% | 95.2% |
| 交易次數 | 2,100 筆 | 1,919 筆 | 1,776 筆 | 1,706 筆 |
| 峰值動用資金 | 162.3%（屬保證金槓桿） | 149.1% | 140.5% | 133.3% |

v3.4 是唯一一次**不是資料驅動**的變動：拿掉 CADJPY（人工判斷 JPY 近期趨勢性變強，不利均值
回歸）、加入 CADCHF/GBPCAD，詳見第 2.6 節。v3.5 單純加入 NZDCAD（實測成本已知、統計特徵
中上，無額外人工判斷），年化再升 0.82 個百分點、Calmar 再降 0.10，見第 2.7 節——同一種
「多分散一檔、換更高絕對報酬」的取捨持續發生，峰值動用資金也持續墊高（133%→162%），
槓桿曝險隨標的數量增加是機械性的結果，值得留意。

（改用 TradingView Pepperstone 真實報價、對齊 MT5 broker 時區後的數字。樣本從 2.78 年縮短為
1.67 年——這是刻意的取捨，見第 6 節「已知限制」；數字會隨 GitHub Actions 每小時重跑而變動，
以網頁即時數字為準，這裡只記錄改版當下的快照。）

逐檔明細：`docs/fx_top8_performance.csv`。互動圖表與逐筆交易明細見網頁本身。

### $100,000 本金可以賺多少？

若部位等比放大（首單 $1,500→$6,000，維持相同風險配置——外匯市場流動性極高，
這個量級不會顯著改變真實點差成本，線性外推是合理假設）：

| 情境 | 金額 |
|---|---|
| **預期年化獲利** | **≈ $10,950 / 年**（10.95% × $100,000） |
| 由 Sharpe 反推的年化波動度 | 10.95% ÷ 2.85 ≈ 3.84%（≈ $3,842） |
| 68% 信心區間（±1 個標準差） | 約 $7,108 ~ $14,792 / 年 |
| 95% 信心區間（±2 個標準差） | 約 $3,266 ~ $18,634 / 年 |
| 歷史最深回撤（同期間內） | ≈ $2,060（帳面浮虧，非年度虧損） |
| 需要的保證金槓桿 | 峰值約 $162,300 |
| 實際手數 | 首單中位 0.085 手、滿倉最大 0.130 手 |

**這是統計期望值，不是保證**，且這次樣本只有 1.67 年，比前一版的 2.78 年更短，統計把握度
更低——MDD/Sharpe 看起來變好一部分是真的資料源更貼近實盤，但也有一部分是樣本期間本身
剛好比較平順、還沒遇到真正的逆風段。上面的信心區間不包含：

- 樣本期間沒出現的真正外匯危機（2015 瑞郎脫鉤單日 30%、2022 英鎊迷你預算崩盤）——
  這類事件下 4 ATR 停損可能無法在停損價成交，實際虧損會遠超過歷史 MDD。
- 實盤執行的滑價、重新報價、延遲，回測無法完全模擬。
- 標的名單本身是全樣本排名結果（v3.4 再疊加一層人工判斷），未來是否仍是最佳組合沒有
  樣本外驗證（見第 4 節）。CADJPY 被拿掉是主觀判斷、不是資料證明；GBPCAD、NZDCAD 剛補
  實測成本才進榜，樣本內的統計把握度是 12 檔裡最低的兩檔。
- 非美元計價交叉盤（AUDCAD、GBPNZD、GBPCHF、EURAUD、CADCHF、GBPCAD、NZDCAD）的美元金額為
  報價序列近似值，報酬率/夏普/Calmar 等相對指標不受影響。
- 點差成本只用了 MT5 broker 近 7 天的真實樣本套用到全部歷史，不是逐時期的真實點差
  （見與使用者的壓力測試討論：點差抓 2~3 倍寬，年化仍有 4%+ 但 Sharpe 會降到 1.5~1.7；
  若每筆都用觀測到的最大點差執行，策略會轉虧，MDD 暴增到雙位數——這是尾部風險，不是常態）。

一句話：**這是一個年化 11%、夏普 2.9 的穩健低頻策略，不是印鈔機**。用 $100,000 操作，
合理預期是一年賺 $7,108~$14,792 這個量級，而不是舊版宣稱的「$40,000~$80,000」。

---

## 6. 實盤部署：ChannelGridDCA_EA.mq5

已依第 1 節邏輯寫成 MT5 EA，放在 `MQL5/Experts/ChannelGridDCA_EA.mq5`，掛在任一張圖表即可同時管理清單內所有商品。

**關鍵設計**

| 項目 | 做法 |
|---|---|
| 手數計算 | `1手名目 = (tick_value ÷ tick_size) × 現價`，MT5 提供的 tick_value 已含跨幣別轉換 |
| 部位大小 | 預設依淨值百分比（首單 6%、加碼 7.2%），帳戶成長時自動放大 |
| 硬停損 | 掛在**經紀商端**，DCA 後自動隨新均價移動；`InpStopAtr=0` 時 EA 拒絕啟動 |
| 止盈 | `min(EMA50, 均價+1ATR)`；因 EMA50 會移動，由 EA 監控，另在均價+1ATR 掛經紀商端 TP 當斷線保護 |
| 點差過濾 | 即時點差 > 該對中位數 ×3 時禁止新開倉（實測極端時段可達中位數的 10~40 倍） |
| 總曝險上限 | 所有部位名目合計 ≤ 淨值 60% |
| 非重繪 | EMA/ATR 一律讀已收盤 4H K 棒，觸價判斷用即時 Bid/Ask |
| 帳戶類型 | 部位以「合計」方式讀取，netting 與 hedging 帳戶皆可運作；層數存於 GlobalVariable，跨重啟保留 |

### 時區與資料來源的演變（實盤持倉跟網頁對不上的真正原因）

實測發現實盤持倉跟網頁回測對不上（8檔中2檔方向/有無完全不同、2檔層數不同）。原因：
**Python 回測（v1）用 UTC 時間切 4H K棒**，但 EA（v1）用 MT5 內建的 `PERIOD_H4`，那是照
**broker 伺服器時區**切的（實測約 UTC+3，非 UTC+0）。同一時刻兩邊看到的 K棒範圍不同 →
EMA/ATR 不同 → 上下軌位置不同 → 觸發進場/加倉/止盈的時間點自然對不上，4層DCA的策略差1根
K棒就足以整層對不齊。

- **v2（已淘汰）**：讓 EA 自己用 `PERIOD_M15` 組出 UTC 對齊的 4H K棒去配合 Python，
  但這樣 EA 邏輯變複雜，且多一個「`InpBrokerGmtOffsetHours` 填錯」的風險點。
- **v3（目前版本）**：反過來讓 **Python 對齊 MT5**。資料來源同時從 yfinance 換成
  **TradingView 的 Pepperstone 報價**（`PEPPERSTONE:EURUSD` 等），理由：
  yfinance 是不綁定特定 broker 的聚合「指示性報價」（Yahoo 自己標註
  `quoteSourceName: 'Delayed Quote'`，且即時 bid/ask 方向還會顛倒），Pepperstone 才是這個策略
  實際下單的 broker。抓資料用的 `tvDatafeed` 套件內部用 `datetime.fromtimestamp()`（不帶時區）
  轉換時間，這個函式會用**執行當下那台機器的本地時區**去解讀 Unix 時間戳——也就是同一份資料，
  在台北時間的電腦、跟在 GitHub Actions 的 UTC 伺服器上跑，naive 時間字串會不一樣。
  解法：`fx_data_pepperstone.py` 每次執行都動態量測「這台機器的本地時區跟 UTC 差幾小時」
  （不是寫死常數），換算回真正 UTC，再加上 broker 時區偏移（`BROKER_GMT_OFFSET_HOURS=3`）
  變成 **MT5 broker 時間**，`resample("4h")` 切出來的邊界因此直接對齊 MT5 的 `PERIOD_H4`。
  EA 也因此改回單純用 `iMA`/`iATR` 讀原生 H4（拿掉 v2 的自建K棒邏輯）——兩邊現在用同一套
  broker 時區時鐘，不需要任何一邊「配合」另一邊做特殊轉換。

**上線前務必做的四件事**：MetaEditor 按 F7 編譯確認 0 errors → 確認經紀商品種代碼是否需要後綴
（如 `AUDCAD.r`）→ 確認 `fx_data_pepperstone.py` 裡的 `BROKER_GMT_OFFSET_HOURS` 是否正確
（有些broker伺服器會隨美國/歐洲夏令時間調整，偏移量可能一年內會變動，需要定期覆核）→
**先用模擬帳戶跑至少一個月**，比對成交紀錄與網頁是否吻合。

**已知限制**：TradingView 上 Pepperstone 的 1H 歷史經 `tvDatafeed`（免登入模式）只能拿到約
600 天（≈1.7年），比 yfinance 的 730 天略短，樣本數因此變少，統計把握度略降，換來的是
資料跟實際下單的 broker 同源、時區可以精確對齊——這是刻意的取捨。

---

## 7. 附錄：關鍵檔案索引

| 檔案 | 內容 |
|---|---|
| `backtest_real_stock_grid_verify.py` | 美股 15m 真實股票驗證引擎 |
| `backtest_htf_grid.py` | 美股 4H／日線驗證引擎 |
| `backtest_fx_grid_faithful.py` | 外匯 4H 忠實版（無 bug）引擎 |
| `fx_grid_optimize.py` / `fx_portfolio_sim.py` | 標的特徵分析、優化方向測試、組合模擬 |
| `fx_tradeable_report.py` | 15 檔篩選後完整報告產生器 |
| `ExportForexRealCosts.mq5` | MT5 腳本：真實歷史點差／隔夜利息調查（v3，取樣統計版） |
| `mt5_forex_real_costs.csv` | 上述腳本實測輸出：G8 全28檔＋XAUUSD 真實點差統計 |
| `fx_engine_v2.py` / `fx_portfolio_v2.py` | 回測引擎：正確跨幣別部位與手數計算、前一根指標、排除換日棒 |
| `fx_data_pepperstone.py` | 資料層：抓 TradingView Pepperstone 報價、動態時區換算成 MT5 broker 時間 |
| `scripts/generate_report.py` | 產生 docs/index.html 的主腳本，GitHub Actions 每小時執行 |
| `docs/fx_top8_performance.csv` / `docs/fx_trade_log.csv` | 最新一次自動更新的逐檔績效／逐筆交易明細 |
| `ChannelGridDCA_EA.mq5` | MT5 實盤 EA v3.5（已部署至 MQL5/Experts，12 檔，無 CADJPY） |
| `TRADING_LOGIC_AND_INSTRUMENTS.md` | 目前運作版本的乾淨規格文件（邏輯＋參數＋12檔商品表） |
| `fx_28pairs_scan_results.csv` / `fx_28pairs_scan_grid.png` | G8 全28檔完整掃描結果與視覺化 |
